[project]
name = "workout-sync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "garminconnect>=0.2.38",
    "python-dotenv>=1.2.2",
    "xlrd>=2.0.2",
]

[project.scripts]
workout-sync = "workout_sync.cli:main"
